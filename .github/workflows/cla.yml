# Contributor License Agreement check.
#
# Every pull request from someone not on the allowlist gets a bot comment
# asking them to sign CLA.md by replying once. Signatures are stored as JSON
# in a branch of this repo, so there is no third-party service holding them.
#
# One-time setup before this can pass:
#   1. Create a classic PAT with `repo` scope on the account that should own
#      the signature file, and add it to this repo as the secret
#      PERSONAL_ACCESS_TOKEN.
#   2. Create an empty orphan branch named `cla-signatures`.
#
# If you use the hosted cla-assistant.io GitHub App instead of this action,
# delete this file — otherwise both will comment on every pull request.
name: CLA

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  actions: write
  contents: read
  pull-requests: write
  statuses: write

jobs:
  cla:
    name: Check the CLA
    runs-on: ubuntu-latest
    # Skips itself cleanly until the secret exists, rather than failing every
    # pull request in the meantime. `secrets` isn't available to a job-level
    # `if`, but `env` is available to a step-level one.
    env:
      PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
    steps:
      - name: CLA assistant
        if: env.PERSONAL_ACCESS_TOKEN != '' && ((github.event.comment.body == 'recheck' || startsWith(github.event.comment.body, 'I have read the CLA Document and I hereby sign the CLA')) || github.event_name == 'pull_request_target')
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          path-to-signatures: signatures/version1/cla.json
          path-to-document: https://github.com/getwriteapp/write/blob/master/CLA.md
          branch: cla-signatures
          allowlist: brettkcherry,dependabot[bot],dependabot-preview[bot]
          custom-notsigned-prcomment: >
            Thank you for the pull request. Before it can be merged, please
            read the [Contributor License Agreement](https://github.com/getwriteapp/write/blob/master/CLA.md)
            and sign it by posting the comment below. It applies to all your
            future contributions here, so you only do this once. You keep the
            copyright in your work.
          custom-pr-sign-comment: 'I have read the CLA Document and I hereby sign the CLA'
          custom-allsigned-prcomment: 'CLA signed. Thank you.'
